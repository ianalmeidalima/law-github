1: Cálculo de Gorjeta
def calcular_gorjeta(valor_conta, porcentagem_gorjeta):
    return valor_conta * (porcentagem_gorjeta / 100)

# Exemplo de uso:
print(f"Gorjeta: R$ {calcular_gorjeta(100, 15):.2f}")



2: Verificador de Palíndromo
def eh_palindromo(texto):
    processado = "".join(char.lower() for char in texto if char.isalnum())
    return "Sim" if processado == processado[::-1] else "Não"

# Exemplo de uso:
print(eh_palindromo("Apos a sopa")) # Retorna: Sim


3: Preço com Desconto
preco_original = float(input("Preço do produto: "))
desconto_pct = float(input("Porcentagem de desconto: "))

valor_desconto = preco_original * (desconto_pct / 100)
preco_final = preco_original - valor_desconto

print(f"Desconto: R$ {valor_desconto:.2f}")
print(f"Preço Final: R$ {round(preco_final, 2)}")

4: Contador de Dias de Vida
from datetime import date

ano = int(input("Ano de nascimento: "))
mes = int(input("Mês de nascimento: "))
dia = int(input("Dia de nascimento: "))

data_nasc = date(ano, mes, dia)
hoje = date.today()
dias_vividos = (hoje - data_nasc).days

print(f"Você está vivo há {dias_vividos} dias.")

