1. Calculadora Básica
num1 = float(input("Primeiro número: "))
op = input("Operação (+, -, *, /): ")
num2 = float(input("Segundo número: "))

if op == '+': print(f"Resultado: {num1 + num2}")
elif op == '-': print(f"Resultado: {num1 - num2}")
elif op == '*': print(f"Resultado: {num1 * num2}")
elif op == '/': print(f"Resultado: {num1 / num2}" if num2 != 0 else "Erro: Divisão por zero")
else: print("Operação inválida")

2. Média da Turma 
notas = []
while True:
    entrada = input("Digite a nota (ou 'sair' para calcular): ")
    if entrada.lower() == 'sair': break
    notas.append(float(entrada))

if notas:
    print(f"Média da turma: {sum(notas) / len(notas):.2f}")
3. Verificador de Senha
senha = input("Digite sua senha: ")
tem_numero = any(char.isdigit() for char in senha)

if len(senha) >= 8 and tem_numero:
    print("Senha segura!")
else:
    print("Senha insegura: deve ter 8+ caracteres e pelo menos 1 número.")
4. Contador de Pares e Ímpares
pares = impares = 0
numeros = input("Digite números separados por espaço: ").split()

for n in numeros:
    if int(n) % 2 == 0: pares += 1
    else: impares += 1

print(f"Pares: {pares} | Ímpares: {impares}")

